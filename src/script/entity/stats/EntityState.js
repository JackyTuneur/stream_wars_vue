/**
 * Entity State is an enumeration that contains the different state of an Entity
 * * So we can add multiple state to an entity if we need to
 */
const EntityState = {
	Idle: 0
}

export default EntityState